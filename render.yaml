services:
  - type: web
    name: theater-scheduler-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn theater_sched.api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0

