# Большой Театр - Планировщик расписания

Протестировать -> http://176.114.88.77/

Система для автоматического планирования расписания спектаклей театра с использованием технологии constraint programming (CP-SAT) от Google OR-Tools. Система решает сложную задачу оптимизации расписания с учетом множества ограничений и предпочтений.

## 📋 Описание

Проект представляет собой полнофункциональное веб-приложение для автоматического составления оптимального расписания театральных спектаклей. Система учитывает:

- **Жёсткие ограничения**: привязка постановок к конкретным сценам, точное количество показов, последовательность показов, выходные дни
- **Мягкие ограничения**: перерывы между разными спектаклями, приоритетные спектакли на выходные дни
- **Управление персоналом**: распределение людей по ролям с балансировкой нагрузки
- **Закреплённые назначения**: возможность предварительно закрепить спектакли за конкретными датами и временем

## ✨ Основные возможности

### Планирование расписания
- ✅ Создание и управление постановками с указанием сцены и количества показов
- ✅ Управление сценами/площадками театра
- ✅ Создание таймслотов (дата + время) для каждой сцены
- ✅ Установка приоритетов для выходных дней
- ✅ Закрепление спектаклей за конкретными датами

### Управление персоналом
- ✅ Добавление людей (артистов, дирижёров и т.д.)
- ✅ Создание ролей для каждой постановки
- ✅ Назначение людей на роли с указанием возможности исполнения
- ✅ Автоматическое распределение людей по ролям с балансировкой нагрузки

### Оптимизация расписания
- ✅ Автоматическое составление оптимального расписания с использованием CP-SAT решателя
- ✅ Настраиваемые ограничения и предпочтения
- ✅ Визуализация результата в виде календаря, таблицы и диаграммы Ганта
- ✅ Просмотр и редактирование назначений людей на роли

### Ограничения решателя

**Жёсткие ограничения** (всегда соблюдаются):
- В каждом таймслоте максимум одна постановка
- Точное количество показов для каждой постановки
- Все показы одного спектакля идут подряд без пропусков
- Понедельник — выходной день
- Постановки могут быть назначены только на таймслоты своей сцены

**Мягкие ограничения** (учитываются в целевой функции):
- Перерыв между разными спектаклями (штраф за отсутствие перерыва)
- Приоритет спектаклей на выходные дни (бонус в целевой функции)
- Заполнение выходных слотов (мягкое ограничение)

## 🏗️ Архитектура

Проект построен по принципам чистой архитектуры с разделением на слои:

```
├── theater_sched/           # Основной модуль приложения
│   ├── api/                # REST API endpoints (FastAPI)
│   │   └── main.py         # API маршруты и валидация
│   ├── domain/             # Доменные модели (Pydantic dataclasses)
│   │   └── models.py       # Production, Stage, TimeSlot, Scenario, etc.
│   ├── solver/             # Логика оптимизации
│   │   └── cp_sat_solver.py # CP-SAT решатель на основе OR-Tools
│   ├── services/           # Бизнес-логика
│   │   ├── scenarios.py    # Сервис управления сценариями
│   │   └── role_generator.py # Генератор ролей
│   └── repositories/       # Слой данных
│       └── memory.py       # In-memory хранилище
├── web/                    # Frontend (SPA)
│   ├── index.html          # Главная страница с UI
│   └── app.js              # Клиентская логика
├── deploy/                 # Конфигурация для развертывания
│   └── nginx/              # Nginx конфигурация
├── requirements.txt        # Python зависимости
└── docker-compose.yml      # Docker Compose для локальной разработки
```

### Технологический стек

**Backend:**
- Python 3.10+
- FastAPI — современный веб-фреймворк для API
- OR-Tools (CP-SAT) — решатель задач constraint programming от Google
- Pydantic — валидация данных и модели
- Uvicorn — ASGI сервер

**Frontend:**
- Vanilla JavaScript — без фреймворков
- HTML5, CSS3 — современная разметка и стили

**Инфраструктура:**
- Docker — контейнеризация
- Docker Compose — оркестрация сервисов
- Nginx — reverse proxy (опционально)

## 📖 Использование

### Создание сценария

1. Откройте веб-интерфейс
2. Во вкладке "Планировщик расписания":
   - Добавьте сцены (например, "Основная сцена", "Камерная сцена")
   - Добавьте постановки с указанием сцены и количества показов
   - Создайте таймслоты (даты и время для каждой сцены)
   - При необходимости добавьте людей, роли и назначения
3. Настройте ограничения во вкладке "Требования"
4. Нажмите "Создать сценарий"

### Составление расписания

1. После создания сценария нажмите "Составить расписание"
2. Система запустит оптимизацию с использованием CP-SAT решателя
3. Результат будет отображён во вкладке "Расписание"

### Просмотр результатов

Расписание можно просматривать в трёх видах:
- **Календарь** — визуальное отображение по дням для каждой сцены
- **Таблица** — структурированный список всех назначений
- **Назначения** — распределение людей по ролям с возможностью редактирования

## 🔌 API Endpoints

### Основные endpoints

- `POST /scenarios` — Создание нового сценария
- `POST /scenarios/{id}/solve` — Запуск оптимизации расписания
- `GET /scenarios/{id}/status` — Получение статуса сценария
- `GET /scenarios/{id}/schedule` — Получение расписания
- `GET /scenarios/{id}/gantt` — Данные для диаграммы Ганта

### Управление данными

- `POST /scenarios/{id}/people` — Добавление человека
- `GET /scenarios/{id}/people` — Список людей
- `POST /scenarios/{id}/roles` — Добавление роли
- `GET /scenarios/{id}/roles` — Список ролей
- `POST /scenarios/{id}/person-production-roles` — Назначение роли человеку
- `GET /scenarios/{id}/assignments` — Получение всех назначений

## 🧮 Алгоритм оптимизации

Система использует CP-SAT (Constraint Programming - Satisfiability) решатель от Google OR-Tools. 

### Математическая модель

**Переменные решения:**
- `x[p,t] ∈ {0,1}` — назначена ли постановка `p` в таймслот `t`

**Целевая функция:**
- Максимизация количества назначений
- Минимизация штрафов за нарушение мягких ограничений
- Максимизация бонусов за приоритетные спектакли в выходные

**Ограничения:**
- Жёсткие: выполняются всегда
- Мягкие: учитываются через штрафы/бонусы в целевой функции

### Особенности реализации

- Поддержка множественных сцен с привязкой постановок
- Последовательные показы (consecutive shows constraint)
- Приоритетные спектакли для выходных дней
- Балансировка нагрузки при распределении людей по ролям
